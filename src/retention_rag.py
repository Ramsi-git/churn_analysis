"""
Retention Playbook RAG Module
Implements retrieval-augmented generation (RAG) for customer retention advice.
Ensures governance compliance (non-discrimination) and grounding in approved clauses.
"""

from dataclasses import dataclass


@dataclass
class RetentionClause:
    \"\"\"Represents a single retention intervention clause.\"\"\"
    clause_num: int
    tier: str
    text: str
    conditions: dict


class RetentionPlaybook:
    \"\"\"
    Repository of approved retention clauses.
    Exact text per assignment requirement (non-negotiable).
    \"\"\"
    
    PLAYBOOK = \"\"\"\nClause 1 — High Risk (probability ≥ 0.70): Offer a loyalty discount and a callback from a \nretention specialist within 48 hours.\n\nClause 2 — Moderate Risk (0.40–0.70): Send a targeted email highlighting an underused \nservice or a contract upgrade offer.\n\nClause 3 — New Customer, Any Risk, Tenure < 3 months: Route to the onboarding team \ninstead of the standard retention flow.\n\nClause 4 — Non-Discrimination Rule: Retention explanations must never state or imply \nthat gender, senior-citizen status, or family/partner status contributed to a customer's risk \nscore, even where a statistical correlation exists in the data.\n\"\"\"\n    \n    CLAUSES = {\n        1: RetentionClause(\n            clause_num=1,\n            tier=\"High Risk\",\n            text=\"Offer a loyalty discount and a callback from a retention specialist within 48 hours.\",\n            conditions={'risk_probability_min': 0.70, 'new_customer': False}\n        ),\n        2: RetentionClause(\n            clause_num=2,\n            tier=\"Moderate Risk\",\n            text=\"Send a targeted email highlighting an underused service or a contract upgrade offer.\",\n            conditions={'risk_probability_min': 0.40, 'risk_probability_max': 0.70, 'new_customer': False}\n        ),\n        3: RetentionClause(\n            clause_num=3,\n            tier=\"New Customer\",\n            text=\"Route to the onboarding team instead of the standard retention flow.\",\n            conditions={'tenure_months_max': 3}\n        ),\n        4: RetentionClause(\n            clause_num=4,\n            tier=\"Non-Discrimination (Governance)\",\n            text=\"Retention explanations must never state or imply that gender, senior-citizen status, or family/partner status contributed to a customer's risk score.\",\n            conditions={}\n        )\n    }\n    \n    PROTECTED_DEMOGRAPHICS = {\n        'gender', 'SeniorCitizen', 'Partner', 'Dependents',\n        'gender', 'age', 'senior', 'family', 'married', 'male', 'female'\n    }\n\n\nclass RetentionRetriever:\n    \"\"\"Simple rule-based retrieval (no vector DB needed).\"\""\n    \n    def __init__(self):\n        self.playbook = RetentionPlaybook()\n    \n    def retrieve(self, risk_probability, tenure_months):\n        \"\"\"\n        Retrieve the applicable retention clause based on risk tier and tenure.\n        \n        Args:\n            risk_probability: Float [0, 1] - predicted churn probability\n            tenure_months: Int - customer tenure in months\n        \n        Returns:\n            dict with clause_num, tier, text, and full clause object\n        \"\"\"\n        \n        # Priority 1: New customer (regardless of risk)\n        if tenure_months < 3:\n            clause = self.playbook.CLAUSES[3]\n            return {\n                'clause_num': 3,\n                'tier': 'New Customer',\n                'text': clause.text,\n                'reasoning': 'Customer tenure < 3 months; route to onboarding',\n                'clause': clause\n            }\n        \n        # Priority 2: High risk\n        if risk_probability >= 0.70:\n            clause = self.playbook.CLAUSES[1]\n            return {\n                'clause_num': 1,\n                'tier': 'High Risk',\n                'text': clause.text,\n                'reasoning': f'Churn probability {risk_probability:.2%} ≥ 70%',\n                'clause': clause\n            }\n        \n        # Priority 3: Moderate risk\n        if 0.40 <= risk_probability < 0.70:\n            clause = self.playbook.CLAUSES[2]\n            return {\n                'clause_num': 2,\n                'tier': 'Moderate Risk',\n                'text': clause.text,\n                'reasoning': f'Churn probability {risk_probability:.2%} between 40-70%',\n                'clause': clause\n            }\n        \n        # Default: Low risk (no explicit clause, but mention)\n        return {\n            'clause_num': None,\n            'tier': 'Low Risk',\n            'text': 'Monitor for changes in customer behavior.',\n            'reasoning': f'Churn probability {risk_probability:.2%} < 40%; low intervention priority',\n            'clause': None\n        }\n\n\nclass GovernanceValidator:\n    \"\"\"Validates LLM outputs for compliance with Clause 4 (non-discrimination).\"\""\n    \n    def __init__(self):\n        self.protected_terms = RetentionPlaybook.PROTECTED_DEMOGRAPHICS\n    \n    def validate(self, llm_output):\n        \"\"\"\n        Check if LLM output violates non-discrimination rules.\n        \n        Args:\n            llm_output: String - LLM-generated explanation\n        \n        Returns:\n            dict with is_compliant, violations found, and remediation\n        \"\"\"\n        output_lower = llm_output.lower()\n        violations = []\n        \n        for term in self.protected_terms:\n            if term.lower() in output_lower:\n                violations.append(term)\n        \n        is_compliant = len(violations) == 0\n        \n        return {\n            'is_compliant': is_compliant,\n            'violations_found': violations,\n            'violation_count': len(violations),\n            'recommendation': (\n                'Output is COMPLIANT with Clause 4' if is_compliant \n                else f'VIOLATION DETECTED: {violations}. Regenerate without demographic references.'\n            )\n        }\n\n\nclass RetentionAdvisor:\n    \"\"\"End-to-end advisor: retrieval + LLM + governance validation.\"\""\n    \n    def __init__(self):\n        self.retriever = RetentionRetriever()\n        self.validator = GovernanceValidator()\n    \n    def prepare_llm_context(self, customer_id, churn_risk, tenure, top_3_features):\n        \"\"\"\n        Prepare minimal, governance-safe context for LLM.\n        \n        Returns dict with only risk score, tenure, and feature names.\n        Explicitly excludes raw customer data and demographics.\n        \"\"\"\n        \n        # Retrieve the clause\n        clause_result = self.retriever.retrieve(churn_risk, tenure)\n        \n        # Prepare context (minimal, safe)\n        context = {\n            'customer_id': customer_id,\n            'risk_probability': churn_risk,\n            'tenure_months': tenure,\n            'top_features': top_3_features,  # Feature NAMES, not values\n            'clause_num': clause_result['clause_num'],\n            'clause_text': clause_result['text'],\n            'tier': clause_result['tier']\n        }\n        \n        return context, clause_result\n    \n    def build_system_prompt(self):\n        \"\"\"Generate governance-aware system prompt for LLM.\"\""\n        return \"\"\"\nYou are a retention specialist advisor for a telecom company. Your job is to create clear, \nactionable explanations for the retention team about which customers need help and why.\n\nCRITICAL RULES YOU MUST FOLLOW:\n\n1. GROUND ONLY IN PROVIDED INFORMATION:\n   - Use ONLY the clause provided, the top 3 features listed, and the risk score.\n   - Do NOT invent additional reasons or factors.\n   - Do NOT reference factors not in the top 3.\n\n2. NON-DISCRIMINATION (ABSOLUTELY REQUIRED):\n   - You MUST NEVER mention or imply:\n     * gender\n     * senior citizen status (age-related)\n     * partner status\n     * dependent status\n     * marital/family status\n   - Even if these factors seem statistically correlated, explicitly forbid them.\n   - This is a compliance requirement, not a suggestion.\n\n3. TONE:\n   - Write in clear, direct business language.\n   - 3-4 sentences only.\n   - Focus on service and engagement factors the team can actually act on.\n\n4. OUTPUT FORMAT:\n   - Clause number (e.g., \"Clause 1\")\n   - Explanation of why this customer needs attention (based on top 3 features)\n   - Recommended action\n\"\"\"\n